# Base Python dependencies, installed into the app's venv by `make install`.
# Pure-Python packages resolved from PyPI (pinned for reproducibility). The GTK
# stack (PyGObject / GTK4 / libadwaita / WebKitGTK / GtkSourceView) stays a
# system dependency and is reached via the venv's --system-site-packages.
Markdown==3.10.2
PyYAML==6.0.3
pymdown-extensions==11.0.1
Pygments==2.20.0
# Web import: main-content extraction (HTML -> markdown), no browser. Pulls lxml
# (a compiled wheel), so the base install is no longer pure-Python.
trafilatura==2.2.0
# Web import tables: markdownify converts a table to a pipe table; nh3 sanitises
# the HTML kept for a table too complex for pipe (col/rowspan, block cells, …).
markdownify==1.2.3
nh3==0.3.6
