# Markdown Vault documentation

- [settings.md](settings.md) — every setting in `settings.yaml`: its dotted
  path, type, default, allowed values and meaning, plus the keys that only take
  effect under another. Generated from
  `src/markdown_vault/core/settings.schema.json`; regenerate with
  `make docs-settings`.
