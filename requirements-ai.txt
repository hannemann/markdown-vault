# Optional AI dependencies (semantic search + local answer generation),
# installed into the venv by `make install-ai`. Heavy, compiled wheels — kept
# opt-in so the base install stays light and can't break on platforms without
# prebuilt wheels.
numpy==2.4.6
onnxruntime==1.27.0
tokenizers==0.23.1
# Multilingual query normalization for hybrid (BM25) retrieval: simplemma
# lemmatizes inflected forms to their base (Gesteinsplaneten -> Gesteinsplanet)
# across 40+ languages, and stopwordsiso supplies stopword sets for 57 languages
# — so lexical matching is language-agnostic instead of hand-tuned per language.
simplemma==2.0.0
stopwordsiso==0.7.1
# llama-cpp-python (the in-process "local" Ask backend) is NOT pinned here — it
# is installed by scripts/install-llama.sh, invoked from `make install-ai`, which
# builds it with Vulkan when the toolchain is present and otherwise fetches the
# prebuilt CPU wheel. Pinning it here would always pull the CPU wheel.
# Document import (File tab): convert PDF/Word/PowerPoint/Excel/audio to Markdown.
# Deliberately torch-free, format-dispatched lightweight backends (~200 MB total,
# no CUDA) instead of docling (3+ GB, half of it CUDA wheels useless on non-NVIDIA
# hardware). No OCR of scanned documents — digital text only.
pymupdf4llm==1.28.2      # PDF  -> Markdown (PyMuPDF), with image extraction
mammoth==1.12.1          # .docx -> Markdown/HTML, with embedded images
python-pptx==1.0.2       # .pptx -> text/notes per slide
openpyxl==3.1.5          # .xlsx -> Markdown tables
faster-whisper==1.2.1    # audio -> transcript (CTranslate2, CPU; model on first use)
